<!DOCTYPE html> 
<html> 
<head> 
    <title>Admin</title>
    <style>
        .center {
            margin-left: auto;
            margin-right: auto;
        }
        table, th, td {
            border: 1px solid black;
            border-collapse: collapse;
            text-align: center;
        }
        img{
            width: 100px;
            height: 100px;   
        }
    </style> 
</head> 
<body> 
  
  <!- For printing variable these 
      tags are used: <%= %>  --> 
  <center><h1> <%= title %> </h1> </center>
     
  <!- For business logic these 
      tags are used: <% %> --> 
  <% if(true){ %> 
  <% } %> 
  <form>
    <center><h2> Videos</h2></center>
    <% for (var i=0; i < data['videos']['sub_categories'].length;  i++ ) { %>
        <center><h3><%=i+1 %>. <%= data['videos']['sub_categories'][i]['name'] %></h3></center>
        <table class="center">
            <tr>
                <td><% var imageUrl = data['photos']['sub_categories'][i]['src'] %><img src=<%= imageUrl%> /></td>
                <td><input type="file" name="test"></input></td>
            </tr>
        </table>
        <br>
        <table class="center">
            <tr>
                <th>Name</th>
                <th>YouTube ID</th>
            <tr>
            <% for (var j=0; j < data['videos']['sub_categories'][i]['items'].length;  j++ ) { %>
                <tr>
                    <td><%= data['videos']['sub_categories'][i]['items'][j]['name'] %></td>	
                    <td><% var value = data['videos']['sub_categories'][i]['items'][j]['id'] %><input type="text" value = <%= value %> /></td>
                </tr>
            <% } %>
        </table>
        <% } %>
        <hr>
        <hr>
        <center><h2> Photos</h2></center>
        <% for (var i=0; i < data['photos']['sub_categories'].length;  i++ ) { %>
        <center><h3><%=i+1 %>. <%= data['photos']['sub_categories'][i]['name'] %></h3></center>
        <table class="center">
            <tr>
                <td><% var imageUrl = data['photos']['sub_categories'][i]['src'] %><img src=<%= imageUrl%> /></td>
                <td><input type="file" name="test"></input></td>
            </tr>
        </table>
        <br>
        <table class="center">
            <tr>
                <th>Name</th>
                <th>Image</th>
                <th>Action</th>
            <tr>
            <% for (var j=0; j < data['photos']['sub_categories'][i]['items'].length;  j++ ) { %>
                <tr>
                    <td><%= data['photos']['sub_categories'][i]['items'][j]['name'] %></td>	
                    <td><% var imageUrl = data['photos']['sub_categories'][i]['items'][j]['src'] %><img src=<%= imageUrl%> /></td>
                    <td><input type="file" name="test"></input></td>
                </tr>
            <% } %>
        </table>
        <% } %>
        <br>
        <center><button>Submit</button></center>
    </form>
</html> 